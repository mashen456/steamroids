/target
**/*.rs.bk
Cargo.lock.bak

# Local env / secrets
.env
.env.*
!.env.example

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# AI agent scratchpad — local-only handover notes for Claude/Cursor sessions.
# Never committed; see .claude/memory.md inside for current findings.
.claude/

# Coverage
*.profraw
tarpaulin-report.html

# Local Windows-MSVC linker workaround — machine-specific absolute SDK path,
# not portable. See the file's header comment. The real fix is repairing the
# 10.0.26100 SDK in the VS Installer.
.cargo/config.toml
# Avatar images downloaded by examples/10_account_dump
avatar_*.jpg

# Refresh-token store written by examples/11_persist_login (contains secrets)
steam_tokens.json
*.tokens.json
